# NewsApp

Recommended: Clean Architecture or MVVM + UseCases.
○
Layers: presentation (Compose/ViewModels), domain (use-cases, models),
data (Retrofit, Room, Paging).
●
Use Kotlin, coroutines, and Flow (no blocking calls on main thread).
●
Dependency Injection: Hilt (preferred) or Dagger2.
